# 認証（ローカルJWT）実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** バックエンドに pytest のテスト基盤を作った上で、メール＋パスワードでのユーザー登録・ログインを実装し、全 API リクエストを JWT で認証する。

**Architecture:** FastAPI 側で HS256 の JWT を発行・検証する。検証は `app/deps.py::get_current_user_id()` の 1 箇所に閉じ込め、既存の全ルーター（`Depends(get_current_user_id)` を 21 箇所で使用）は変更しない。フロントは localStorage にトークンを置き、生成クライアントの `Configuration` に注入することで全 API 呼び出しに一括で Bearer ヘッダーを付ける。

**Tech Stack:** FastAPI / SQLAlchemy 2 / Pydantic v2 / `bcrypt` 5.0 / `python-jose` 3.5 / pytest / Next.js 16.2.9 (App Router) / openapi-generator typescript-fetch

**設計書:** `docs/superpowers/specs/2026-07-31-auth-design.md`

## Global Constraints

- **テストはバックエンドのみ（pytest）。** フロントエンドのテストフレームワークは導入しない。フロントの検証はブラウザ操作の手順で行う。
- **テスト基盤を認証実装より先に作る。** Task 1〜2 で土台と既存機能の回帰テストを整え、Task 3 以降の認証実装は「失敗するテストを書く → 実装する → 通す」の順で進める。
- テストは **PostgreSQL 上の別 DB `misnote_test`** に対して実行する。モデルが `sqlalchemy.dialects.postgresql.UUID` と Postgres ネイティブ ENUM を使っているため SQLite は使えない。
- 依存の追加は本プランで明記したもの（`bcrypt` 追加 / `passlib` 削除 / `pydantic[email]` / `pytest` / `httpx`）**のみ**。それ以外を勝手に足さない。
- 既存コードのリファクタ・整形はしない。差分は各タスクの要求に直接ひもづく行のみ。
- バックエンドは `backend/.venv` の uvicorn、DB のみ Docker（`docker compose up -d db`）。
- Alembic マイグレーションの追加は**不要**（`users.email` / `users.password_hash` は初期マイグレーション `aaf1f33b3324` に既にある）。
- 他人のリソースへのアクセスは **403 ではなく 404** を返す（既存ルーターの挙動に揃える）。
- Next.js 16.2.9 は訓練データと API が異なる。新しい Next.js API を使う前に `frontend/node_modules/next/dist/docs/` を確認すること。本プランで使う `usePathname` / `useRouter`（`next/navigation`）は既存コード（`sidebar.tsx`, `register-form.tsx`）で使用実績があるため確認済み。
- コミットメッセージは既存の慣習（`feat:` / `fix:` / `test:` / `docs:` プレフィックス）に合わせ、末尾に `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` を付ける。

## File Structure

**テスト（新規）**

| ファイル | 責務 |
|---|---|
| `backend/requirements-dev.txt` | テスト専用の依存。`Dockerfile` は `requirements.txt` しか入れないので本番イメージは太らない |
| `backend/pytest.ini` | pytest 設定（testpaths, pythonpath） |
| `backend/tests/conftest.py` | テスト DB の準備、トランザクション巻き戻し、`TestClient` と fixture |
| `backend/tests/test_subjects.py` | 科目 CRUD の回帰テスト |
| `backend/tests/test_units.py` | 単元 CRUD の回帰テスト（Task 6 で所有者チェックを追記） |
| `backend/tests/test_mistake_notes.py` | 中核ドメイン規則（ノート自動生成・wrong_count・correct_streak・mastery_suggested）の回帰テスト |
| `backend/tests/test_auth_utils.py` | `app/auth.py` の単体テスト |
| `backend/tests/test_auth_api.py` | register / login / me のテスト |
| `backend/tests/test_auth_required.py` | 全エンドポイントが認証必須であることのテスト |
| `backend/tests/test_ownership.py` | ユーザー間のデータ分離のテスト |

**バックエンド（新規）**

| ファイル | 責務 |
|---|---|
| `backend/app/auth.py` | パスワードハッシュと JWT の純粋関数のみ。DB・リクエストに依存しない |
| `backend/app/schemas/auth.py` | 登録・ログイン・トークン・ユーザーの Pydantic スキーマ |
| `backend/app/routers/auth.py` | `/v1/auth/register`, `/login`, `/me` |

**バックエンド（変更）**

| ファイル | 変更内容 |
|---|---|
| `backend/requirements.txt` | passlib 削除、bcrypt 追加、pydantic に email extras |
| `backend/app/deps.py` | `get_current_user_id()` を JWT 検証に差し替え、`SEED_USER_ID` 削除 |
| `backend/app/main.py` | auth ルーター登録、`lifespan`（seed 呼び出し）削除 |
| `backend/app/routers/units.py` | 4 エンドポイントに所有者チェックを追加 |
| `backend/app/seed.py` | **削除** |

**フロントエンド（新規）**

| ファイル | 責務 |
|---|---|
| `frontend/src/lib/auth-token.ts` | localStorage の読み書き。localStorage に触るのはこのファイルだけ |
| `frontend/src/components/auth/auth-card.tsx` | ログイン／登録画面の共通の枠（ロゴ＋中央カード） |
| `frontend/src/components/auth/login-form.tsx` | ログインフォーム |
| `frontend/src/components/auth/signup-form.tsx` | 登録フォーム |
| `frontend/src/components/auth/auth-gate.tsx` | 認証状態による出し分けとリダイレクト |
| `frontend/src/components/account/account-panel.tsx` | アカウント情報表示とログアウト |
| `frontend/src/app/login/page.tsx` `signup/page.tsx` `account/page.tsx` | 薄いページ（既存画面と同じ形） |

**フロントエンド（変更）**

| ファイル | 変更内容 |
|---|---|
| `frontend/src/lib/api.ts` | `Configuration` に `accessToken` と 401 middleware、`authApi` 追加 |
| `frontend/src/app/layout.tsx` | `AppShell` を `AuthGate` に差し替え |
| `frontend/src/components/layout/nav-items.tsx` | 「アカウント」を追加 |
| `frontend/src/generated/**` | `npm run generate` による再生成 |

---

## Task 1: pytest のテスト基盤

**Files:**
- Create: `backend/requirements-dev.txt`
- Create: `backend/pytest.ini`
- Create: `backend/tests/conftest.py`
- Create: `backend/tests/test_smoke.py`

**Interfaces:**
- Consumes: 既存の `app.main.app`, `app.database.Base`, `app.deps.get_db` / `get_current_user_id`, `app.models.user.User`
- Produces: 以降の全テストが使う fixture
  - `engine`（session scope）— `misnote_test` DB への Engine
  - `db_session` — テストごとに巻き戻される `Session`
  - `user` / `other_user` — DB に作成済みの `User`
  - `anon_client` — `get_db` のみ差し替えた `TestClient`
  - `client` — さらに `get_current_user_id` を `user.id` に差し替えた `TestClient`

**設計のポイント:** `client` fixture が `get_current_user_id` を**依存オーバーライド**で差し替える。こうしておくと、Task 5 でこの関数の中身が JWT 検証に変わっても、既存機能のテストは 1 行も直さずに通り続ける。認証そのもののテストだけが `anon_client`（オーバーライドなし）を使う。

- [ ] **Step 1: `backend/requirements-dev.txt` を作る**

```
-r requirements.txt
pytest>=8.0.0
httpx>=0.27.0
```

`httpx` は FastAPI の `TestClient` が内部で使う。`Dockerfile` は `requirements.txt` しか
インストールしないので、本番イメージには入らない。

- [ ] **Step 2: インストールして SQLAlchemy のバージョンを確認する**

Run:
```bash
cd backend && .venv/bin/pip install -r requirements-dev.txt && \
  .venv/bin/python -c "import sqlalchemy, pytest, httpx; print('sqlalchemy', sqlalchemy.__version__)"
```
Expected: エラーなく終わり、`sqlalchemy 2.x` が出る。
（`conftest.py` が使う `join_transaction_mode` は SQLAlchemy 2.0 以降の機能。1.x が出たら止まって報告すること。）

- [ ] **Step 3: `backend/pytest.ini` を作る**

```ini
[pytest]
testpaths = tests
pythonpath = .
```

`pythonpath = .` が無いと `from app...` の import が解決しない。

- [ ] **Step 4: `backend/tests/conftest.py` を作る**

```python
import os
from uuid import uuid4

import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine, text
from sqlalchemy.orm import Session

import app.models  # noqa: F401 — 全モデルを Base に登録するため
from app.database import Base
from app.deps import get_current_user_id, get_db
from app.main import app as fastapi_app
from app.models.user import User

# 開発用 DB とは別の DB を使う。開発データを壊さないため。
TEST_DATABASE_URL = os.environ.get(
    "TEST_DATABASE_URL", "postgresql://misnote:misnote@localhost:5432/misnote_test"
)


def _ensure_test_database() -> None:
    """misnote_test が無ければ作る。CREATE DATABASE はトランザクション内で実行できない。"""
    base_url, db_name = TEST_DATABASE_URL.rsplit("/", 1)
    admin_engine = create_engine(f"{base_url}/postgres", isolation_level="AUTOCOMMIT")
    with admin_engine.connect() as conn:
        exists = conn.execute(
            text("SELECT 1 FROM pg_database WHERE datname = :name"), {"name": db_name}
        ).scalar()
        if not exists:
            conn.execute(text(f'CREATE DATABASE "{db_name}"'))
    admin_engine.dispose()


@pytest.fixture(scope="session")
def engine():
    _ensure_test_database()
    eng = create_engine(TEST_DATABASE_URL)
    # 前回の残骸を消してから作り直す（Alembic は通さない。速度優先）
    Base.metadata.drop_all(eng)
    Base.metadata.create_all(eng)
    yield eng
    eng.dispose()


@pytest.fixture()
def db_session(engine):
    """外側のトランザクションに参加させ、テストごとに丸ごと巻き戻す。

    ルーター側が db.commit() を呼んでも、create_savepoint モードなら
    セーブポイントの解放になるだけで、外側の rollback で全部消える。
    """
    connection = engine.connect()
    transaction = connection.begin()
    session = Session(bind=connection, join_transaction_mode="create_savepoint")
    yield session
    session.close()
    transaction.rollback()
    connection.close()


@pytest.fixture()
def user(db_session) -> User:
    u = User(email=f"user-{uuid4().hex[:8]}@example.com", name="テストユーザー")
    db_session.add(u)
    db_session.commit()
    return u


@pytest.fixture()
def other_user(db_session) -> User:
    u = User(email=f"other-{uuid4().hex[:8]}@example.com", name="別のユーザー")
    db_session.add(u)
    db_session.commit()
    return u


@pytest.fixture()
def anon_client(db_session):
    """認証していないクライアント。

    with 文で使わないのは lifespan を走らせないため。Task 5 で lifespan は
    無くなるが、それまでは seed が開発用 DB を触ってしまう。
    """
    fastapi_app.dependency_overrides[get_db] = lambda: db_session
    client = TestClient(fastapi_app)
    yield client
    fastapi_app.dependency_overrides.clear()


@pytest.fixture()
def client(anon_client, user):
    """user としてログイン済みのクライアント。

    get_current_user_id を丸ごと差し替えるので、この関数の実装が
    シードユーザー返却から JWT 検証に変わっても、このテストは影響を受けない。
    """
    fastapi_app.dependency_overrides[get_current_user_id] = lambda: user.id
    return anon_client
```

- [ ] **Step 5: `backend/tests/test_smoke.py` を書く**

```python
def test_health_returns_ok(anon_client):
    response = anon_client.get("/health")
    assert response.status_code == 200
    assert response.json() == {"status": "ok"}


def test_client_fixture_is_scoped_to_its_user(client, user):
    """client fixture が user として振る舞い、他人のデータが混ざらないこと。"""
    created = client.post("/v1/subjects", json={"name": "数学"})
    assert created.status_code == 201

    listed = client.get("/v1/subjects")
    assert listed.status_code == 200
    assert [s["name"] for s in listed.json()] == ["数学"]


def test_each_test_starts_from_a_clean_database(db_session):
    """前のテストで作った科目が DB に残っていないこと（巻き戻しの確認）。

    /v1/subjects を見るだけでは不十分。user fixture が毎回別のユーザーを作るので、
    巻き戻しが効いていなくても一覧は空になる。テーブル全体の件数で見る。
    """
    from app.models.subject import Subject

    assert db_session.query(Subject).count() == 0
```

- [ ] **Step 6: DB を起動してテストを実行する**

Run:
```bash
cd /Users/towa/Developer/myApp/misnote && docker compose up -d db
cd backend && .venv/bin/pytest -v
```
Expected: 3 件すべて PASS。
（`test_each_test_starts_from_a_clean_database` が FAIL する場合は巻き戻しが効いていない。
`join_transaction_mode="create_savepoint"` の指定漏れを疑うこと。）

- [ ] **Step 7: 開発用 DB が汚れていないことを確認する**

Run:
```bash
cd backend && .venv/bin/python -c "
from app.database import SessionLocal
from app.models.subject import Subject
db = SessionLocal()
print('dev subjects with name 数学:', db.query(Subject).filter(Subject.name=='数学').count())
"
```
Expected: テストで作った「数学」が開発用 DB に**入っていない**こと（テスト実行前と同じ件数）。

- [ ] **Step 8: `.gitignore` を確認する**

Run:
```bash
cd backend && cat .gitignore
```
`__pycache__` や `.pytest_cache` が無視対象に無ければ、`.pytest_cache/` を 1 行追加する。

- [ ] **Step 9: コミット**

```bash
cd /Users/towa/Developer/myApp/misnote
git add backend/requirements-dev.txt backend/pytest.ini backend/tests backend/.gitignore
git commit -m "$(cat <<'EOF'
test: add pytest infrastructure for the backend

PostgreSQL 上の別DB misnote_test に対して実行し、テストごとに
トランザクションを巻き戻す。client fixture が get_current_user_id を
依存オーバーライドで差し替えるため、認証導入後もテストは無変更で通る。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 既存機能の回帰テスト

**Files:**
- Create: `backend/tests/test_subjects.py`
- Create: `backend/tests/test_units.py`
- Create: `backend/tests/test_mistake_notes.py`

**Interfaces:**
- Consumes: Task 1 の `client` / `db_session` fixture
- Produces: なし（テストのみ）

**目的:** 認証導入は全エンドポイントに影響する。壊れたときに検知できる網を先に張る。

- [ ] **Step 1: `backend/tests/test_subjects.py` を書く**

```python
def test_create_and_list_subject(client):
    created = client.post("/v1/subjects", json={"name": "数学"})
    assert created.status_code == 201
    assert created.json()["name"] == "数学"

    listed = client.get("/v1/subjects")
    assert listed.status_code == 200
    assert len(listed.json()) == 1


def test_update_subject(client):
    subject_id = client.post("/v1/subjects", json={"name": "数学"}).json()["id"]

    updated = client.put(f"/v1/subjects/{subject_id}", json={"name": "算数"})
    assert updated.status_code == 200
    assert updated.json()["name"] == "算数"


def test_delete_subject(client):
    subject_id = client.post("/v1/subjects", json={"name": "数学"}).json()["id"]

    assert client.delete(f"/v1/subjects/{subject_id}").status_code == 204
    assert client.get("/v1/subjects").json() == []


def test_delete_subject_with_units_returns_409(client):
    subject_id = client.post("/v1/subjects", json={"name": "数学"}).json()["id"]
    client.post(f"/v1/subjects/{subject_id}/units", json={"name": "二次方程式"})

    response = client.delete(f"/v1/subjects/{subject_id}")
    assert response.status_code == 409
    assert response.json()["detail"] == "Subject has related units or questions"


def test_update_unknown_subject_returns_404(client):
    response = client.put(
        "/v1/subjects/00000000-0000-0000-0000-000000000099", json={"name": "無い"}
    )
    assert response.status_code == 404
```

- [ ] **Step 2: `backend/tests/test_units.py` を書く**

```python
import pytest


@pytest.fixture()
def subject_id(client) -> str:
    return client.post("/v1/subjects", json={"name": "数学"}).json()["id"]


def test_create_and_list_unit(client, subject_id):
    created = client.post(f"/v1/subjects/{subject_id}/units", json={"name": "二次方程式"})
    assert created.status_code == 201

    listed = client.get(f"/v1/subjects/{subject_id}/units")
    assert listed.status_code == 200
    assert [u["name"] for u in listed.json()] == ["二次方程式"]


def test_update_unit(client, subject_id):
    unit_id = client.post(
        f"/v1/subjects/{subject_id}/units", json={"name": "二次方程式"}
    ).json()["id"]

    updated = client.put(f"/v1/units/{unit_id}", json={"name": "因数分解"})
    assert updated.status_code == 200
    assert updated.json()["name"] == "因数分解"


def test_delete_unit(client, subject_id):
    unit_id = client.post(
        f"/v1/subjects/{subject_id}/units", json={"name": "二次方程式"}
    ).json()["id"]

    assert client.delete(f"/v1/units/{unit_id}").status_code == 204
    assert client.get(f"/v1/subjects/{subject_id}/units").json() == []


def test_delete_unit_with_questions_returns_409(client, subject_id):
    unit_id = client.post(
        f"/v1/subjects/{subject_id}/units", json={"name": "二次方程式"}
    ).json()["id"]
    client.post(
        "/v1/questions",
        json={
            "subject_id": subject_id,
            "unit_id": unit_id,
            "question_text": "x^2-5x+6=0 を解け",
            "memo": "因数分解の形を見落とした",
        },
    )

    response = client.delete(f"/v1/units/{unit_id}")
    assert response.status_code == 409


def test_question_with_unit_from_another_subject_returns_400(client, subject_id):
    other_subject_id = client.post("/v1/subjects", json={"name": "英語"}).json()["id"]
    foreign_unit_id = client.post(
        f"/v1/subjects/{other_subject_id}/units", json={"name": "文法"}
    ).json()["id"]

    response = client.post(
        "/v1/questions",
        json={
            "subject_id": subject_id,
            "unit_id": foreign_unit_id,
            "question_text": "ちぐはぐな組み合わせ",
            "memo": "メモ",
        },
    )
    assert response.status_code == 400
```

- [ ] **Step 3: `backend/tests/test_mistake_notes.py` を書く**

このアプリの中核ルール。`routers/attempts.py` と `routers/mistake_notes.py` にまたがっている。

```python
import pytest

MASTERY_THRESHOLD = 3


@pytest.fixture()
def question_id(client) -> str:
    subject_id = client.post("/v1/subjects", json={"name": "数学"}).json()["id"]
    return client.post(
        "/v1/questions",
        json={
            "subject_id": subject_id,
            "question_text": "x^2-5x+6=0 を解け",
            "memo": "因数分解の形を見落とした",
        },
    ).json()["id"]


def _only_note(client) -> dict:
    notes = client.get("/v1/mistake-notes").json()
    assert len(notes) == 1
    return notes[0]


def test_creating_a_question_with_memo_creates_a_note(client, question_id):
    note = _only_note(client)
    assert note["question"]["id"] == question_id
    assert note["status"] == "active"
    assert note["wrong_count"] == 1
    assert note["correct_streak"] == 0


def test_incorrect_attempt_increments_wrong_count_without_duplicating_the_note(
    client, question_id
):
    client.post(f"/v1/questions/{question_id}/attempts", json={"is_correct": False})

    note = _only_note(client)
    assert note["wrong_count"] == 2
    assert note["correct_streak"] == 0


def test_correct_attempt_increments_the_streak(client, question_id):
    response = client.post(
        f"/v1/questions/{question_id}/attempts",
        json={"is_correct": True, "user_answer": "x=2, 3"},
    )
    assert response.status_code == 201
    assert response.json()["correct_streak"] == 1
    assert response.json()["mastery_suggested"] is False


def test_three_correct_attempts_suggest_mastery_without_changing_status(
    client, question_id
):
    for _ in range(MASTERY_THRESHOLD - 1):
        response = client.post(
            f"/v1/questions/{question_id}/attempts", json={"is_correct": True}
        )
        assert response.json()["mastery_suggested"] is False

    response = client.post(
        f"/v1/questions/{question_id}/attempts", json={"is_correct": True}
    )
    assert response.json()["correct_streak"] == MASTERY_THRESHOLD
    assert response.json()["mastery_suggested"] is True

    # 提案どまりで、自動では mastered にならない
    assert _only_note(client)["status"] == "active"


def test_incorrect_attempt_resets_the_streak(client, question_id):
    client.post(f"/v1/questions/{question_id}/attempts", json={"is_correct": True})
    client.post(f"/v1/questions/{question_id}/attempts", json={"is_correct": False})

    assert _only_note(client)["correct_streak"] == 0


def test_marking_mastered_moves_the_note_between_lists(client, question_id):
    note_id = _only_note(client)["id"]

    response = client.put(f"/v1/mistake-notes/{note_id}/status", json={"status": "mastered"})
    assert response.status_code == 200

    assert client.get("/v1/mistake-notes").json() == []
    mastered = client.get("/v1/mistake-notes/mastered").json()
    assert len(mastered) == 1
    assert mastered[0]["next_review_at"] is None


def test_incorrect_attempt_reverts_a_mastered_note_to_active(client, question_id):
    note_id = _only_note(client)["id"]
    client.put(f"/v1/mistake-notes/{note_id}/status", json={"status": "mastered"})

    client.post(f"/v1/questions/{question_id}/attempts", json={"is_correct": False})

    assert client.get("/v1/mistake-notes/mastered").json() == []
    assert _only_note(client)["status"] == "active"


def test_today_excludes_notes_without_a_review_date(client, question_id):
    assert client.get("/v1/mistake-notes/today").json() == []


def test_today_includes_notes_due_today(client, question_id):
    from datetime import date

    note_id = _only_note(client)["id"]
    client.put(
        f"/v1/mistake-notes/{note_id}",
        json={"next_review_at": date.today().isoformat()},
    )

    today = client.get("/v1/mistake-notes/today").json()
    assert [n["id"] for n in today] == [note_id]
```

- [ ] **Step 4: テストを実行する**

Run:
```bash
cd backend && .venv/bin/pytest -v
```
Expected: 全件 PASS。

**FAIL したテストがあれば、テストを実装に合わせて曲げる前に、どちらが正しいかを報告すること。**
`CLAUDE.md` の「Mistake-note / mastery rules」が仕様の記述であり、実装がそれと食い違っているなら
それはバグの発見であって、テストの書き間違いとは限らない。

- [ ] **Step 5: コミット**

```bash
cd /Users/towa/Developer/myApp/misnote
git add backend/tests
git commit -m "$(cat <<'EOF'
test: cover subjects, units and the mistake-note rules

認証の導入は全エンドポイントに影響するため、先に回帰テストを用意する。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 依存関係の入れ替えと `app/auth.py`

**Files:**
- Modify: `backend/requirements.txt`
- Create: `backend/tests/test_auth_utils.py`
- Create: `backend/app/auth.py`

**Interfaces:**
- Consumes: `app.config.settings.secret_key`（既存）
- Produces:
  - `hash_password(plain: str) -> str`
  - `verify_password(plain: str, hashed: str) -> bool`
  - `create_access_token(user_id: UUID) -> str`
  - `decode_access_token(token: str) -> UUID | None`
  - 定数 `ALGORITHM = "HS256"`, `ACCESS_TOKEN_EXPIRE_DAYS = 7`

**背景:** `passlib 1.7.4` はインストール済みの `bcrypt 5.0.0` と組み合わせて動作しない。passlib は bcrypt バックエンド初期化時に 72 バイト超のパスワードで既知バグ検査を行うが、bcrypt 5.0 はそれを切り詰めず `ValueError` を投げるため、こちらのコードがハッシュ化を呼ぶ前に落ちる。

- [ ] **Step 1: 現状の破壊を再現して確認する**

Run:
```bash
cd backend && .venv/bin/python -c "
from passlib.context import CryptContext
print(CryptContext(schemes=['bcrypt']).hash('x'))
"
```
Expected: FAIL — `ValueError: password cannot be longer than 72 bytes` で終わる。
（これが passlib を外す理由。直っていた場合も方針は変えず次へ進む。）

- [ ] **Step 2: requirements.txt を書き換える**

`backend/requirements.txt` の 3 行を変更する。

- `passlib[bcrypt]>=1.7.4` の行を **削除**
- `bcrypt>=5.0.0` の行を **追加**（削除した行と同じ位置）
- `pydantic>=2.10.0` を `pydantic[email]>=2.10.0` に **変更**

`python-multipart` は本設計では使わないが、既存の依存であり本タスクと無関係なので**残す**。

- [ ] **Step 3: インストールする**

Run:
```bash
cd backend && .venv/bin/pip install -r requirements-dev.txt
```
Expected: `bcrypt` と `email_validator` が入り、エラーなく終わる。

- [ ] **Step 4: 失敗するテスト `backend/tests/test_auth_utils.py` を書く**

```python
from datetime import datetime, timedelta, timezone
from uuid import uuid4

from jose import jwt

from app.auth import (
    ALGORITHM,
    create_access_token,
    decode_access_token,
    hash_password,
    verify_password,
)
from app.config import settings


def test_hash_is_not_the_plain_password():
    hashed = hash_password("mypassword123")
    assert hashed != "mypassword123"
    assert hashed.startswith("$2b$")


def test_the_same_password_hashes_differently_each_time():
    """ソルトが効いていること。"""
    assert hash_password("mypassword123") != hash_password("mypassword123")


def test_verify_accepts_the_right_password_and_rejects_others():
    hashed = hash_password("mypassword123")
    assert verify_password("mypassword123", hashed) is True
    assert verify_password("wrongpassword", hashed) is False


def test_verify_returns_false_instead_of_raising_on_oversized_input():
    """bcrypt は 72 バイト超で ValueError を投げる。ログインは 500 ではなく失敗にする。"""
    hashed = hash_password("mypassword123")
    assert verify_password("あ" * 100, hashed) is False


def test_verify_returns_false_on_a_malformed_hash():
    assert verify_password("mypassword123", "not-a-bcrypt-hash") is False


def test_token_round_trips_the_user_id():
    user_id = uuid4()
    assert decode_access_token(create_access_token(user_id)) == user_id


def test_tampered_token_is_rejected():
    token = create_access_token(uuid4())
    assert decode_access_token(token[:-3] + "aaa") is None


def test_token_signed_with_another_key_is_rejected():
    forged = jwt.encode(
        {"sub": str(uuid4()), "exp": datetime.now(timezone.utc) + timedelta(days=1)},
        "some-other-secret",
        algorithm=ALGORITHM,
    )
    assert decode_access_token(forged) is None


def test_expired_token_is_rejected():
    expired = jwt.encode(
        {
            "sub": str(uuid4()),
            "exp": datetime.now(timezone.utc) - timedelta(seconds=1),
        },
        settings.secret_key,
        algorithm=ALGORITHM,
    )
    assert decode_access_token(expired) is None


def test_garbage_token_is_rejected():
    assert decode_access_token("not.a.token") is None


def test_token_payload_carries_nothing_but_sub_and_exp():
    """JWT の中身は誰でも読める。余計な情報を入れていないこと。"""
    token = create_access_token(uuid4())
    payload = jwt.decode(token, settings.secret_key, algorithms=[ALGORITHM])
    assert set(payload) == {"sub", "exp"}
```

- [ ] **Step 5: テストが失敗することを確認する**

Run:
```bash
cd backend && .venv/bin/pytest tests/test_auth_utils.py -v
```
Expected: FAIL — `ModuleNotFoundError: No module named 'app.auth'` で収集エラーになる。

- [ ] **Step 6: `backend/app/auth.py` を作る**

```python
from datetime import datetime, timedelta, timezone
from uuid import UUID

import bcrypt
from jose import JWTError, jwt

from app.config import settings

ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_DAYS = 7


def hash_password(plain: str) -> str:
    return bcrypt.hashpw(plain.encode("utf-8"), bcrypt.gensalt()).decode("utf-8")


def verify_password(plain: str, hashed: str) -> bool:
    # bcrypt は 72 バイト超のパスワードや壊れたハッシュで ValueError を投げる。
    # ログインは検証失敗として扱えばよく、500 にする必要はない。
    try:
        return bcrypt.checkpw(plain.encode("utf-8"), hashed.encode("utf-8"))
    except ValueError:
        return False


def create_access_token(user_id: UUID) -> str:
    expire = datetime.now(timezone.utc) + timedelta(days=ACCESS_TOKEN_EXPIRE_DAYS)
    payload = {"sub": str(user_id), "exp": expire}
    return jwt.encode(payload, settings.secret_key, algorithm=ALGORITHM)


def decode_access_token(token: str) -> UUID | None:
    # 欠落・改ざん・期限切れ・sub が UUID でない、を全て None に畳む
    try:
        payload = jwt.decode(token, settings.secret_key, algorithms=[ALGORITHM])
        return UUID(payload["sub"])
    except (JWTError, KeyError, ValueError):
        return None
```

- [ ] **Step 7: テストが通ることを確認する**

Run:
```bash
cd backend && .venv/bin/pytest -v
```
Expected: 全件 PASS（既存の回帰テストを含む）。

- [ ] **Step 8: コミット**

```bash
cd /Users/towa/Developer/myApp/misnote
git add backend/requirements.txt backend/app/auth.py backend/tests/test_auth_utils.py
git commit -m "$(cat <<'EOF'
feat: add password hashing and JWT helpers

passlib 1.7.4 はインストール済みの bcrypt 5.0.0 と組み合わせると
バックエンド初期化時に ValueError で落ちるため、bcrypt を直接使う。
EmailStr 用に pydantic[email] へ変更。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 登録・ログイン・自分情報のエンドポイント

**Files:**
- Create: `backend/tests/test_auth_api.py`
- Create: `backend/app/schemas/auth.py`
- Create: `backend/app/routers/auth.py`
- Modify: `backend/app/main.py`（import 行とルーター登録を 1 行ずつ）

**Interfaces:**
- Consumes: Task 3 の `hash_password` / `verify_password` / `create_access_token`、既存の `app.deps.get_db` / `get_current_user_id`、既存モデル `app.models.user.User`
- Produces:
  - スキーマ `UserRegister`, `UserLogin`, `TokenResponse`, `UserResponse`
  - エンドポイント `POST /v1/auth/register`, `POST /v1/auth/login`, `GET /v1/auth/me`
  - ルーター関数名は `register` / `login` / `get_me`（生成クライアントのメソッド名がこれに由来する）

**このタスクの時点では既存 API はまだ無防備のまま。** `get_current_user_id()` は依然シードユーザーを返すので、フロントエンドは壊れない。

- [ ] **Step 1: 失敗するテスト `backend/tests/test_auth_api.py` を書く**

```python
import pytest

VALID = {"email": "student@example.com", "password": "password123", "name": "テスト太郎"}


def _register(anon_client, **overrides):
    return anon_client.post("/v1/auth/register", json={**VALID, **overrides})


def test_register_creates_a_user(anon_client):
    response = _register(anon_client)
    assert response.status_code == 201

    body = response.json()
    assert body["email"] == VALID["email"]
    assert body["name"] == VALID["name"]
    assert "id" in body


def test_register_never_leaks_the_password(anon_client):
    body = _register(anon_client).json()
    assert "password" not in body
    assert "password_hash" not in body


def test_register_rejects_a_duplicate_email(anon_client):
    _register(anon_client)

    response = _register(anon_client, name="別人")
    assert response.status_code == 409
    assert response.json()["detail"] == "Email already registered"


@pytest.mark.parametrize(
    "overrides",
    [
        {"password": "1234567"},  # 8文字未満
        {"email": "not-an-email"},
        {"name": ""},
        {"password": "あ" * 30},  # 90バイト = bcrypt の 72 バイト上限超え
    ],
)
def test_register_rejects_invalid_input_with_422(anon_client, overrides):
    assert _register(anon_client, **overrides).status_code == 422


def test_login_returns_a_bearer_token(anon_client):
    _register(anon_client)

    response = anon_client.post(
        "/v1/auth/login",
        json={"email": VALID["email"], "password": VALID["password"]},
    )
    assert response.status_code == 200

    body = response.json()
    assert body["token_type"] == "bearer"
    assert body["access_token"].count(".") == 2  # JWT は3パート


def test_login_with_a_wrong_password_is_rejected(anon_client):
    _register(anon_client)

    response = anon_client.post(
        "/v1/auth/login", json={"email": VALID["email"], "password": "wrongpassword"}
    )
    assert response.status_code == 401


def test_login_does_not_reveal_whether_the_email_exists(anon_client):
    """未登録メールとパスワード違いで、応答が区別できないこと。"""
    _register(anon_client)

    wrong_password = anon_client.post(
        "/v1/auth/login", json={"email": VALID["email"], "password": "wrongpassword"}
    )
    unknown_email = anon_client.post(
        "/v1/auth/login", json={"email": "nobody@example.com", "password": "password123"}
    )

    assert wrong_password.status_code == unknown_email.status_code == 401
    assert wrong_password.json() == unknown_email.json()


def test_login_issues_a_token_for_the_registered_user(anon_client):
    from app.auth import decode_access_token

    user_id = _register(anon_client).json()["id"]
    token = anon_client.post(
        "/v1/auth/login",
        json={"email": VALID["email"], "password": VALID["password"]},
    ).json()["access_token"]

    assert str(decode_access_token(token)) == user_id


def test_me_returns_the_current_user(client, user):
    response = client.get("/v1/auth/me")
    assert response.status_code == 200
    assert response.json()["id"] == str(user.id)
    assert response.json()["email"] == user.email
```

- [ ] **Step 2: テストが失敗することを確認する**

Run:
```bash
cd backend && .venv/bin/pytest tests/test_auth_api.py -v
```
Expected: FAIL — 全件 404（ルートが無い）。

- [ ] **Step 3: `backend/app/schemas/auth.py` を作る**

```python
from uuid import UUID

from pydantic import BaseModel, EmailStr, Field, field_validator

# bcrypt が受け付ける上限。日本語は UTF-8 で 1 文字 3 バイトなので、
# 文字数ではなくバイト数で見ないと 72 文字でも上限を超える。
MAX_PASSWORD_BYTES = 72


class UserRegister(BaseModel):
    email: EmailStr
    password: str = Field(min_length=8)
    name: str = Field(min_length=1)

    @field_validator("password")
    @classmethod
    def password_fits_bcrypt(cls, value: str) -> str:
        if len(value.encode("utf-8")) > MAX_PASSWORD_BYTES:
            raise ValueError(f"password must be at most {MAX_PASSWORD_BYTES} bytes")
        return value


class UserLogin(BaseModel):
    email: EmailStr
    password: str


class TokenResponse(BaseModel):
    access_token: str
    token_type: str = "bearer"


class UserResponse(BaseModel):
    id: UUID
    email: str
    name: str

    model_config = {"from_attributes": True}
```

- [ ] **Step 4: `backend/app/routers/auth.py` を作る**

```python
from uuid import UUID

from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session

from app.auth import create_access_token, hash_password, verify_password
from app.deps import get_current_user_id, get_db
from app.models.user import User
from app.schemas.auth import TokenResponse, UserLogin, UserRegister, UserResponse

router = APIRouter()


@router.post("/register", response_model=UserResponse, status_code=status.HTTP_201_CREATED)
def register(body: UserRegister, db: Session = Depends(get_db)) -> User:
    if db.query(User).filter(User.email == body.email).first():
        raise HTTPException(status_code=409, detail="Email already registered")
    user = User(
        email=body.email,
        name=body.name,
        password_hash=hash_password(body.password),
    )
    db.add(user)
    db.commit()
    db.refresh(user)
    return user


@router.post("/login", response_model=TokenResponse)
def login(body: UserLogin, db: Session = Depends(get_db)) -> TokenResponse:
    user = db.query(User).filter(User.email == body.email).first()
    # 「メール未登録」と「パスワード不一致」を区別しない
    # ─ どのメールが登録済みかを外部に漏らさないため
    if not user or not user.password_hash or not verify_password(body.password, user.password_hash):
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Incorrect email or password",
            headers={"WWW-Authenticate": "Bearer"},
        )
    return TokenResponse(access_token=create_access_token(user.id))


@router.get("/me", response_model=UserResponse)
def get_me(
    user_id: UUID = Depends(get_current_user_id),
    db: Session = Depends(get_db),
) -> User:
    user = db.get(User, user_id)
    if not user:
        raise HTTPException(status_code=404, detail="User not found")
    return user
```

- [ ] **Step 5: `backend/app/main.py` にルーターを登録する**

import 行を変更（`auth` を追加）:
```python
from app.routers import attempts, auth, mistake_notes, questions, subjects
```

`include_router` の並びの**先頭**に 1 行追加:
```python
app.include_router(auth.router,            prefix="/v1/auth",          tags=["auth"])
```

`lifespan` や seed 呼び出しはこのタスクでは触らない（Task 5 で扱う）。

- [ ] **Step 6: テストが通ることを確認する**

Run:
```bash
cd backend && .venv/bin/pytest -v
```
Expected: 全件 PASS。

- [ ] **Step 7: コミット**

```bash
cd /Users/towa/Developer/myApp/misnote
git add backend/app/schemas/auth.py backend/app/routers/auth.py backend/app/main.py \
        backend/tests/test_auth_api.py
git commit -m "$(cat <<'EOF'
feat: add register / login / me endpoints

この時点では既存エンドポイントは未保護のまま（Task 5 で保護する）。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 全 API を認証必須にする + seed の削除

**Files:**
- Create: `backend/tests/test_auth_required.py`
- Modify: `backend/app/deps.py`
- Modify: `backend/app/main.py`（`lifespan` と seed の import を削除）
- Delete: `backend/app/seed.py`

**Interfaces:**
- Consumes: Task 3 の `decode_access_token`
- Produces: `get_current_user_id()` が JWT 由来の `UUID` を返すようになる。呼び出し側から見たシグネチャは変わらない（`Depends(get_current_user_id)` のまま）ので、**既存ルーターは 1 行も変更しない**

**このタスク完了時点でフロントエンドの全画面が 401 で壊れる。想定内。** Task 8 以降で直る。

- [ ] **Step 1: 失敗するテスト `backend/tests/test_auth_required.py` を書く**

`client` fixture は依存オーバーライドで認証を迂回するので、ここでは使わない。
すべて `anon_client` に対して実際のトークンを付けて検証する。

```python
import pytest

PROTECTED_PATHS = [
    "/v1/subjects",
    "/v1/questions",
    "/v1/mistake-notes",
    "/v1/mistake-notes/today",
    "/v1/mistake-notes/mastered",
    "/v1/auth/me",
]

CREDENTIALS = {"email": "student@example.com", "password": "password123"}


@pytest.fixture()
def token(anon_client) -> str:
    anon_client.post("/v1/auth/register", json={**CREDENTIALS, "name": "テスト太郎"})
    return anon_client.post("/v1/auth/login", json=CREDENTIALS).json()["access_token"]


@pytest.mark.parametrize("path", PROTECTED_PATHS)
def test_request_without_a_token_is_rejected(anon_client, path):
    assert anon_client.get(path).status_code == 401


@pytest.mark.parametrize("path", PROTECTED_PATHS)
def test_request_with_a_garbage_token_is_rejected(anon_client, path):
    response = anon_client.get(path, headers={"Authorization": "Bearer garbage"})
    assert response.status_code == 401


@pytest.mark.parametrize("path", PROTECTED_PATHS)
def test_request_with_a_valid_token_is_accepted(anon_client, token, path):
    response = anon_client.get(path, headers={"Authorization": f"Bearer {token}"})
    assert response.status_code == 200


def test_missing_header_returns_401_not_403(anon_client):
    """HTTPBearer の既定は 403。設計どおり 401 に揃っていること。"""
    assert anon_client.get("/v1/subjects").status_code == 401


def test_writes_are_protected_too(anon_client):
    assert anon_client.post("/v1/subjects", json={"name": "数学"}).status_code == 401


def test_health_stays_public(anon_client):
    assert anon_client.get("/health").status_code == 200


def test_register_and_login_stay_public(anon_client):
    registered = anon_client.post(
        "/v1/auth/register", json={**CREDENTIALS, "name": "テスト太郎"}
    )
    assert registered.status_code == 201
    assert anon_client.post("/v1/auth/login", json=CREDENTIALS).status_code == 200


def test_the_openapi_schema_advertises_bearer_auth(anon_client):
    schema = anon_client.get("/openapi.json").json()
    assert "HTTPBearer" in schema["components"]["securitySchemes"]
    assert "security" in schema["paths"]["/v1/subjects"]["get"]
    assert "security" not in schema["paths"]["/v1/auth/login"]["post"]
```

- [ ] **Step 2: テストが失敗することを確認する**

Run:
```bash
cd backend && .venv/bin/pytest tests/test_auth_required.py -v
```
Expected: `test_request_without_a_token_is_rejected` などが FAIL（現状 200 が返る）。

- [ ] **Step 3: `backend/app/deps.py` を書き換える**

ファイル全体を次に置き換える。

```python
from collections.abc import Generator
from uuid import UUID

from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPAuthorizationCredentials, HTTPBearer
from sqlalchemy.orm import Session

from app.auth import decode_access_token
from app.database import SessionLocal

# auto_error=False にしている理由:
# HTTPBearer の既定はヘッダー欠落時に 403 を返す。設計では 401 に揃えたいので、
# credentials が None のケースを自分で処理する。
bearer_scheme = HTTPBearer(auto_error=False)


def get_db() -> Generator[Session, None, None]:
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()


def get_current_user_id(
    credentials: HTTPAuthorizationCredentials | None = Depends(bearer_scheme),
) -> UUID:
    user_id = decode_access_token(credentials.credentials) if credentials else None
    if user_id is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Invalid or expired token",
            headers={"WWW-Authenticate": "Bearer"},
        )
    return user_id
```

`SEED_USER_ID` 定数は無くなる。

- [ ] **Step 4: `backend/app/main.py` から seed を外す**

削除する import:
```python
from contextlib import asynccontextmanager
from app.database import SessionLocal
from app.seed import ensure_seed_subjects, ensure_seed_user
```

削除する `lifespan` 関数全体（デコレータ含む）:
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    ...
    yield
```

`FastAPI(...)` の引数から `lifespan=lifespan` を外す:
```python
app = FastAPI(title="misnote API", version="0.1.0")
```

- [ ] **Step 5: `backend/app/seed.py` を削除する**

Run:
```bash
cd /Users/towa/Developer/myApp/misnote && git rm backend/app/seed.py
```

- [ ] **Step 6: テストが通ることを確認する**

Run:
```bash
cd backend && .venv/bin/pytest -v
```
Expected: 全件 PASS。

**特に重要:** Task 2 で書いた既存機能のテスト（`test_subjects.py` など）が**1 行も変更せずに**
通ること。`client` fixture が `get_current_user_id` を依存オーバーライドしているため、
実装が JWT 検証に変わっても影響を受けない。ここが FAIL する場合は
`deps.py` の関数名を変えてしまっていないか確認すること。

- [ ] **Step 7: コミット**

Step 5 の `git rm` で削除は既にステージされている。

```bash
cd /Users/towa/Developer/myApp/misnote
git add backend/app/deps.py backend/app/main.py backend/tests/test_auth_required.py
git commit -m "$(cat <<'EOF'
feat: require a JWT on every API endpoint

get_current_user_id() を JWT 検証に差し替えた。全ルーターが既に
Depends 経由でこの関数を使っているため、ルーター側の変更はない。
固定シードユーザーが不要になったので seed.py と lifespan を削除。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: `units.py` の所有者チェック

**Files:**
- Create: `backend/tests/test_ownership.py`
- Modify: `backend/app/routers/units.py`

**Interfaces:**
- Consumes: `app.deps.get_current_user_id`（`units.py` では import 済みだが未使用）、`app.models.subject.Subject`（新規 import）
- Produces: `_get_owned_subject(db, subject_id, user_id) -> Subject`, `_get_owned_unit(db, unit_id, user_id) -> Unit`（`units.py` 内のローカルヘルパー）

**背景:** `units.py` の 4 エンドポイントには所有者チェックが一切なく、他人の `subject_id` を指定すれば単元を読み書きできる。ユーザーが 1 人の間は実害がないが、認証を入れた今はここだけがデータ分離の穴になる。

- [ ] **Step 1: 失敗するテスト `backend/tests/test_ownership.py` を書く**

2 人のユーザーそれぞれのクライアントが要る。`conftest.py` の `client` は `user` 固定なので、
このファイル内でローカルの fixture を組む。

```python
import pytest

from app.deps import get_current_user_id
from app.main import app as fastapi_app


@pytest.fixture()
def as_user(anon_client, user):
    """user として振る舞うクライアント。"""
    fastapi_app.dependency_overrides[get_current_user_id] = lambda: user.id
    return anon_client


@pytest.fixture()
def switch_to(anon_client):
    """呼ぶたびに現在のユーザーを切り替えるヘルパー。"""

    def _switch(target):
        fastapi_app.dependency_overrides[get_current_user_id] = lambda: target.id
        return anon_client

    return _switch


def test_subjects_are_not_visible_to_other_users(as_user, switch_to, other_user):
    as_user.post("/v1/subjects", json={"name": "数学"})

    assert switch_to(other_user).get("/v1/subjects").json() == []


def test_another_users_subject_cannot_be_updated(as_user, switch_to, other_user):
    subject_id = as_user.post("/v1/subjects", json={"name": "数学"}).json()["id"]

    response = switch_to(other_user).put(f"/v1/subjects/{subject_id}", json={"name": "乗っ取り"})
    assert response.status_code == 404


def test_units_of_another_users_subject_cannot_be_listed(as_user, switch_to, other_user):
    subject_id = as_user.post("/v1/subjects", json={"name": "数学"}).json()["id"]

    response = switch_to(other_user).get(f"/v1/subjects/{subject_id}/units")
    assert response.status_code == 404


def test_units_cannot_be_created_under_another_users_subject(as_user, switch_to, other_user):
    subject_id = as_user.post("/v1/subjects", json={"name": "数学"}).json()["id"]

    response = switch_to(other_user).post(
        f"/v1/subjects/{subject_id}/units", json={"name": "侵入した単元"}
    )
    assert response.status_code == 404


def test_another_users_unit_cannot_be_updated_or_deleted(as_user, switch_to, other_user):
    subject_id = as_user.post("/v1/subjects", json={"name": "数学"}).json()["id"]
    unit_id = as_user.post(
        f"/v1/subjects/{subject_id}/units", json={"name": "二次方程式"}
    ).json()["id"]

    intruder = switch_to(other_user)
    assert intruder.put(f"/v1/units/{unit_id}", json={"name": "乗っ取り"}).status_code == 404
    assert intruder.delete(f"/v1/units/{unit_id}").status_code == 404


def test_the_owner_can_still_manage_their_units(as_user):
    subject_id = as_user.post("/v1/subjects", json={"name": "数学"}).json()["id"]
    unit_id = as_user.post(
        f"/v1/subjects/{subject_id}/units", json={"name": "二次方程式"}
    ).json()["id"]

    assert as_user.get(f"/v1/subjects/{subject_id}/units").status_code == 200
    assert as_user.put(f"/v1/units/{unit_id}", json={"name": "因数分解"}).status_code == 200
    assert as_user.delete(f"/v1/units/{unit_id}").status_code == 204


def test_questions_and_notes_are_not_visible_to_other_users(as_user, switch_to, other_user):
    subject_id = as_user.post("/v1/subjects", json={"name": "数学"}).json()["id"]
    as_user.post(
        "/v1/questions",
        json={
            "subject_id": subject_id,
            "question_text": "x^2-5x+6=0 を解け",
            "memo": "因数分解の形を見落とした",
        },
    )

    intruder = switch_to(other_user)
    assert intruder.get("/v1/questions").json() == []
    assert intruder.get("/v1/mistake-notes").json() == []
```

- [ ] **Step 2: テストが失敗することを確認する**

Run:
```bash
cd backend && .venv/bin/pytest tests/test_ownership.py -v
```
Expected: units 関連の 3 件（list / create / update・delete）が FAIL する。
科目・問題・ノートのテストは既に通る（それらのルーターには所有者チェックがあるため）。
**これが塞ぐべき穴の正確な範囲。**

- [ ] **Step 3: `backend/app/routers/units.py` を書き換える**

ファイル全体を次に置き換える。

```python
from uuid import UUID

from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session

from app.deps import get_db, get_current_user_id
from app.models.subject import Subject
from app.models.unit import Unit
from app.schemas.unit import UnitCreate, UnitResponse, UnitUpdate

# /v1/subjects/{subject_id}/units 配下
subjects_router = APIRouter()

# /v1/units/{id} 配下
units_router = APIRouter()


def _get_owned_subject(db: Session, subject_id: UUID, user_id: UUID) -> Subject:
    subject = db.query(Subject).filter(Subject.id == subject_id, Subject.user_id == user_id).first()
    if not subject:
        raise HTTPException(status_code=404, detail="Subject not found")
    return subject


def _get_owned_unit(db: Session, unit_id: UUID, user_id: UUID) -> Unit:
    unit = (
        db.query(Unit)
        .join(Subject, Unit.subject_id == Subject.id)
        .filter(Unit.id == unit_id, Subject.user_id == user_id)
        .first()
    )
    if not unit:
        raise HTTPException(status_code=404, detail="Unit not found")
    return unit


@subjects_router.get("/{subject_id}/units", response_model=list[UnitResponse])
def list_units(
    subject_id: UUID,
    user_id: UUID = Depends(get_current_user_id),
    db: Session = Depends(get_db),
) -> list[Unit]:
    _get_owned_subject(db, subject_id, user_id)
    return db.query(Unit).filter(Unit.subject_id == subject_id).all()


@subjects_router.post("/{subject_id}/units", response_model=UnitResponse, status_code=status.HTTP_201_CREATED)
def create_unit(
    subject_id: UUID,
    body: UnitCreate,
    user_id: UUID = Depends(get_current_user_id),
    db: Session = Depends(get_db),
) -> Unit:
    _get_owned_subject(db, subject_id, user_id)
    unit = Unit(subject_id=subject_id, name=body.name)
    db.add(unit)
    db.commit()
    db.refresh(unit)
    return unit


@units_router.put("/{unit_id}", response_model=UnitResponse)
def update_unit(
    unit_id: UUID,
    body: UnitUpdate,
    user_id: UUID = Depends(get_current_user_id),
    db: Session = Depends(get_db),
) -> Unit:
    unit = _get_owned_unit(db, unit_id, user_id)
    unit.name = body.name
    db.commit()
    db.refresh(unit)
    return unit


@units_router.delete("/{unit_id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_unit(
    unit_id: UUID,
    user_id: UUID = Depends(get_current_user_id),
    db: Session = Depends(get_db),
) -> None:
    unit = _get_owned_unit(db, unit_id, user_id)
    if unit.questions:
        raise HTTPException(status_code=409, detail="Unit has related questions")
    db.delete(unit)
    db.commit()
```

- [ ] **Step 4: テストが通ることを確認する**

Run:
```bash
cd backend && .venv/bin/pytest -v
```
Expected: 全件 PASS。Task 2 の `test_units.py`（本人による正常操作）も引き続き通ること。

- [ ] **Step 5: コミット**

```bash
cd /Users/towa/Developer/myApp/misnote
git add backend/app/routers/units.py backend/tests/test_ownership.py
git commit -m "$(cat <<'EOF'
fix: scope unit endpoints to the owning user

units.py だけ所有者チェックが無く、他人の subject_id を指定すれば
単元を読み書きできる状態だった。他のルーターに合わせて 404 を返す。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: `SECRET_KEY` の差し替えと開発用 DB のリセット

**Files:**
- Modify: `backend/.env`（gitignore 済み。コミットしない）

**Interfaces:**
- Consumes: なし
- Produces: なし（環境の変更のみ）

> **破壊的操作を含む。Step 3 の実行前に必ずユーザーへ最終確認を取ること。**
> 開発用 DB の科目・単元・問題・苦手ノート・回答履歴が全て消える。
> （テスト用 DB `misnote_test` は毎回作り直されるので影響しない。）

- [ ] **Step 1: `SECRET_KEY` をランダム値に差し替える**

Run:
```bash
cd backend && .venv/bin/python -c "import secrets; print('SECRET_KEY=' + secrets.token_urlsafe(48))"
```

出力された行で `backend/.env` の `SECRET_KEY=change-me-in-production` を置き換える。
`backend/.env.example` は**プレースホルダのまま変更しない**（コミット対象なので実値を入れない）。

- [ ] **Step 2: 鍵を変えてもテストが通ることを確認する**

Run:
```bash
cd backend && .venv/bin/pytest -v
```
Expected: 全件 PASS。（テストは毎回その場でトークンを発行するので、鍵の値には依存しない。
ここが FAIL するなら鍵をハードコードしているテストがある。）

- [ ] **Step 3: ユーザーに最終確認を取ってから開発用 DB をリセットする**

「開発用 DB を初期化します。現在の科目・問題・苦手ノートは全て消えます。実行してよいですか？」
と確認し、**承認が取れてから**実行する。

Run:
```bash
cd /Users/towa/Developer/myApp/misnote
docker compose down -v
docker compose up -d db
cd backend && .venv/bin/alembic upgrade head
```
Expected: `alembic upgrade head` が `Running upgrade -> aaf1f33b3324` で成功する。

- [ ] **Step 4: 空の DB であることと、seed が復活しないことを確認する**

Run:
```bash
cd backend && .venv/bin/uvicorn app.main:app &
sleep 3
.venv/bin/python -c "
from app.database import SessionLocal
from app.models.user import User
from app.models.subject import Subject
db = SessionLocal()
print('users   :', db.query(User).count())
print('subjects:', db.query(Subject).count())
"
```
Expected: `users: 0`、`subjects: 0`（seed 削除済みなので起動してもユーザーは作られない）。
uvicorn は起動したままにしておく（Task 8 で使う）。

- [ ] **Step 5: コミットは無し**

`.env` は gitignore されており、他に変更ファイルは無い。

Run:
```bash
cd /Users/towa/Developer/myApp/misnote && git status --short
```
Expected: 出力が空。

---

## Task 8: 生成クライアントの更新とトークンの配線

**Files:**
- Modify: `backend/openapi.json`（再取得）
- Modify: `frontend/src/generated/**`（`npm run generate` による再生成）
- Create: `frontend/src/lib/auth-token.ts`
- Modify: `frontend/src/lib/api.ts`

**Interfaces:**
- Consumes: Task 4 の `/v1/auth/*`、Task 5 の HTTPBearer セキュリティスキーム
- Produces:
  - `getToken(): string | null`, `setToken(token: string): void`, `clearToken(): void`
  - `authApi` シングルトン（`@/lib/api` から export）
  - 全 API 呼び出しに `Authorization: Bearer` が自動付与される

- [ ] **Step 1: openapi.json を取り直して再生成する**

uvicorn が起動していることを確認してから実行する。

Run:
```bash
cd /Users/towa/Developer/myApp/misnote
curl -s http://localhost:8000/openapi.json -o backend/openapi.json
cd frontend && npm run generate
```
Expected: 生成が成功し、`src/generated/apis/AuthApi.ts` と
`src/generated/models/{UserRegister,UserLogin,TokenResponse,UserResponse}.ts` ができる。
`npm run generate` は末尾で `scripts/strip-instanceof.mjs` を実行するので、
`instanceOf*` 型ガードは自動的に除去される（これが無いと `npm run build` が TS7053 で落ちる）。

- [ ] **Step 2: 生成されたメソッド名を確認する**

**以降のコードはこの出力に合わせること。** 予想と違えば実際の名前を使う。

Run:
```bash
cd frontend && grep -n "async [a-zA-Z]*(" src/generated/apis/AuthApi.ts | grep -v Raw
```
Expected: `loginV1AuthLoginPost`, `registerV1AuthRegisterPost`, `getMeV1AuthMeGet` の 3 つ。

- [ ] **Step 3: `frontend/src/lib/auth-token.ts` を作る**

```ts
// JWT の保管場所。localStorage に触るのはこのファイルだけに閉じる。
const TOKEN_KEY = "misnote_token";

export function getToken(): string | null {
  // SSR 中は window が無い
  if (typeof window === "undefined") return null;
  return window.localStorage.getItem(TOKEN_KEY);
}

export function setToken(token: string): void {
  window.localStorage.setItem(TOKEN_KEY, token);
}

export function clearToken(): void {
  window.localStorage.removeItem(TOKEN_KEY);
}
```

- [ ] **Step 4: `frontend/src/lib/api.ts` を書き換える**

ファイル全体を次に置き換える。

```ts
import {
  Configuration,
  AuthApi,
  SubjectsApi,
  UnitsApi,
  QuestionsApi,
  MistakeNotesApi,
  AttemptsApi,
} from "@/generated";
import { clearToken, getToken } from "@/lib/auth-token";

const config = new Configuration({
  basePath: process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://localhost:8000",
  // 生成クライアントが全リクエストに Authorization: Bearer を付ける
  accessToken: () => getToken() ?? "",
  middleware: [
    {
      // 期限切れ・不正なトークンの受け口をここ1箇所に集約する。
      // ログイン失敗の 401 でループしないよう、/login にいるときは遷移しない。
      post: async ({ response }) => {
        if (
          response.status === 401 &&
          typeof window !== "undefined" &&
          window.location.pathname !== "/login"
        ) {
          clearToken();
          window.location.replace("/login");
        }
        return response;
      },
    },
  ],
});

export const authApi = new AuthApi(config);
export const subjectsApi = new SubjectsApi(config);
export const unitsApi = new UnitsApi(config);
export const questionsApi = new QuestionsApi(config);
export const mistakeNotesApi = new MistakeNotesApi(config);
export const attemptsApi = new AttemptsApi(config);
```

- [ ] **Step 5: 型チェック・lint・ビルド**

Run:
```bash
cd frontend && npx tsc --noEmit && npm run lint && npm run build
```
Expected: すべてエラー 0。
（`src/generated/` で TS7053 が 50 件以上出る場合は `strip-instanceof.mjs` が効いていない。
Step 1 の `npm run generate` をやり直す。）

- [ ] **Step 6: コミット**

```bash
cd /Users/towa/Developer/myApp/misnote
git add backend/openapi.json frontend/src/generated frontend/src/lib/auth-token.ts frontend/src/lib/api.ts
git commit -m "$(cat <<'EOF'
feat: send the JWT from the generated API client

Configuration に accessToken と 401 ハンドリングの middleware を足した
ので、全 API 呼び出しに一括で効く。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: ログイン画面と新規登録画面

**Files:**
- Create: `frontend/src/app/login/page.tsx`
- Create: `frontend/src/app/signup/page.tsx`
- Create: `frontend/src/components/auth/auth-card.tsx`
- Create: `frontend/src/components/auth/login-form.tsx`
- Create: `frontend/src/components/auth/signup-form.tsx`

**Interfaces:**
- Consumes: Task 8 の `authApi` / `setToken`、既存の `inputBase` / `labelBase`（`@/lib/form-styles`）
- Produces: `/login` と `/signup` のルート。ログイン成功で localStorage にトークンが入り `/` へ遷移する

この時点ではまだ `AuthGate` が無いので、両画面にサイドバーが出る。Task 10 で消える。

- [ ] **Step 1: `frontend/src/components/auth/auth-card.tsx` を作る**

```tsx
// ログイン／新規登録で共通の枠。ロゴ付きの中央カード。
export default function AuthCard({
  title,
  children,
}: {
  title: string;
  children: React.ReactNode;
}) {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center px-5 py-10">
      <div className="font-serif text-[28px] font-bold tracking-[0.05em] text-navy mb-7">
        mis<span className="text-amber">note</span>
      </div>
      <div className="w-full max-w-[380px] bg-surface border border-border rounded-lg p-7">
        <h1 className="font-serif text-[19px] font-bold tracking-[0.02em] text-text mb-6">
          {title}
        </h1>
        {children}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: `frontend/src/components/auth/login-form.tsx` を作る**

```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import AuthCard from "@/components/auth/auth-card";
import { authApi } from "@/lib/api";
import { setToken } from "@/lib/auth-token";
import { inputBase, labelBase } from "@/lib/form-styles";

export default function LoginForm() {
  const router = useRouter();
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [error, setError] = useState("");
  const [submitting, setSubmitting] = useState(false);

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setError("");
    setSubmitting(true);
    try {
      const token = await authApi.loginV1AuthLoginPost({
        userLogin: { email, password },
      });
      setToken(token.accessToken);
      router.replace("/");
    } catch {
      setError("メールアドレスまたはパスワードが正しくありません。");
      setSubmitting(false);
    }
  }

  return (
    <AuthCard title="ログイン">
      <form onSubmit={handleSubmit} className="flex flex-col gap-[18px]">
        {error && (
          <p className="bg-amber-lt border border-amber text-amber text-[13px] rounded-md px-3 py-2.5">
            {error}
          </p>
        )}
        <div>
          <label className={labelBase} htmlFor="email">
            メールアドレス
          </label>
          <input
            id="email"
            type="email"
            className={inputBase}
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            autoComplete="email"
            required
          />
        </div>
        <div>
          <label className={labelBase} htmlFor="password">
            パスワード
          </label>
          <input
            id="password"
            type="password"
            className={inputBase}
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            autoComplete="current-password"
            required
          />
        </div>
        <button
          type="submit"
          disabled={submitting}
          className="w-full rounded-md bg-amber py-2.5 text-[14px] font-bold text-white transition-colors duration-150 hover:bg-amber-dk disabled:opacity-50"
        >
          {submitting ? "ログイン中…" : "ログイン"}
        </button>
        <p className="text-center text-[13px] text-muted">
          アカウントをお持ちでない方は{" "}
          <Link href="/signup" className="font-bold text-amber hover:underline">
            新規登録
          </Link>
        </p>
      </form>
    </AuthCard>
  );
}
```

- [ ] **Step 3: `frontend/src/components/auth/signup-form.tsx` を作る**

登録成功後にそのままログインしてホームへ入る（register はトークンを返さないため）。

```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import AuthCard from "@/components/auth/auth-card";
import { authApi } from "@/lib/api";
import { setToken } from "@/lib/auth-token";
import { inputBase, labelBase } from "@/lib/form-styles";
import { ResponseError } from "@/generated";

export default function SignupForm() {
  const router = useRouter();
  const [name, setName] = useState("");
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [error, setError] = useState("");
  const [submitting, setSubmitting] = useState(false);

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setError("");
    setSubmitting(true);
    try {
      await authApi.registerV1AuthRegisterPost({
        userRegister: { email, password, name },
      });
      const token = await authApi.loginV1AuthLoginPost({
        userLogin: { email, password },
      });
      setToken(token.accessToken);
      router.replace("/");
    } catch (e) {
      if (e instanceof ResponseError && e.response.status === 409) {
        setError("このメールアドレスは既に登録されています。");
      } else {
        setError("登録に失敗しました。入力内容を確認してください。");
      }
      setSubmitting(false);
    }
  }

  return (
    <AuthCard title="新規登録">
      <form onSubmit={handleSubmit} className="flex flex-col gap-[18px]">
        {error && (
          <p className="bg-amber-lt border border-amber text-amber text-[13px] rounded-md px-3 py-2.5">
            {error}
          </p>
        )}
        <div>
          <label className={labelBase} htmlFor="name">
            お名前
          </label>
          <input
            id="name"
            type="text"
            className={inputBase}
            value={name}
            onChange={(e) => setName(e.target.value)}
            required
          />
        </div>
        <div>
          <label className={labelBase} htmlFor="email">
            メールアドレス
          </label>
          <input
            id="email"
            type="email"
            className={inputBase}
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            autoComplete="email"
            required
          />
        </div>
        <div>
          <label className={labelBase} htmlFor="password">
            パスワード
          </label>
          <input
            id="password"
            type="password"
            className={inputBase}
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            autoComplete="new-password"
            minLength={8}
            required
          />
          <p className="mt-1.5 text-[11px] text-muted">8文字以上</p>
        </div>
        <button
          type="submit"
          disabled={submitting}
          className="w-full rounded-md bg-amber py-2.5 text-[14px] font-bold text-white transition-colors duration-150 hover:bg-amber-dk disabled:opacity-50"
        >
          {submitting ? "登録中…" : "登録する"}
        </button>
        <p className="text-center text-[13px] text-muted">
          既にアカウントをお持ちの方は{" "}
          <Link href="/login" className="font-bold text-amber hover:underline">
            ログイン
          </Link>
        </p>
      </form>
    </AuthCard>
  );
}
```

- [ ] **Step 4: ページを 2 つ作る**

`frontend/src/app/login/page.tsx`:
```tsx
import LoginForm from "@/components/auth/login-form";

export default function LoginPage() {
  return <LoginForm />;
}
```

`frontend/src/app/signup/page.tsx`:
```tsx
import SignupForm from "@/components/auth/signup-form";

export default function SignupPage() {
  return <SignupForm />;
}
```

- [ ] **Step 5: 型チェックと lint**

Run:
```bash
cd frontend && npx tsc --noEmit && npm run lint
```
Expected: どちらもエラー 0。
（`ResponseError` は `src/generated/runtime.ts` で定義され `index.ts` から再 export されている。
同じ判定パターンが `src/components/subjects/subjects-manager.tsx:70` に既にある。）

- [ ] **Step 6: ブラウザで登録とログインを通す**

`npm run dev` と uvicorn を起動した状態で、`http://localhost:3000/signup` を開く。

1. 名前・メール・パスワード（8文字以上）を入れて「登録する」→ ホーム `/` に遷移する
2. DevTools の Application → Local Storage に `misnote_token` が入っている
3. 同じメールで再度 `/signup` → 「このメールアドレスは既に登録されています。」が出る
4. `/login` でパスワードを間違える → 「メールアドレスまたはパスワードが正しくありません。」が出て、`/login` に留まる（リダイレクトループが起きない）
5. 正しいパスワードでログイン → ホームに遷移する

Expected: 5 項目すべて成立する。ホームは科目 0 件なので空状態になる（正常）。

- [ ] **Step 7: コミット**

```bash
cd /Users/towa/Developer/myApp/misnote
git add frontend/src/app/login frontend/src/app/signup frontend/src/components/auth
git commit -m "$(cat <<'EOF'
feat: add login and signup screens

登録エンドポイントはトークンを返さないため、登録成功後に続けて
ログインを呼んでホームへ入る。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: 認証ガード・アカウント画面・ログアウト

**Files:**
- Create: `frontend/src/components/auth/auth-gate.tsx`
- Create: `frontend/src/components/account/account-panel.tsx`
- Create: `frontend/src/app/account/page.tsx`
- Modify: `frontend/src/app/layout.tsx`
- Modify: `frontend/src/components/layout/nav-items.tsx`

**Interfaces:**
- Consumes: Task 8 の `getToken` / `clearToken` / `authApi`、既存の `AppShell`
- Produces: 未ログイン時に `/login` へ飛ばすガード。`/account` にログアウトボタン

- [ ] **Step 1: `frontend/src/components/auth/auth-gate.tsx` を作る**

```tsx
"use client";

import { useEffect, useState } from "react";
import { usePathname, useRouter } from "next/navigation";
import AppShell from "@/components/layout/app-shell";
import { getToken } from "@/lib/auth-token";

// 認証なしで見られるページ
const PUBLIC_PATHS = ["/login", "/signup"];

export default function AuthGate({ children }: { children: React.ReactNode }) {
  const pathname = usePathname();
  const router = useRouter();
  // localStorage はサーバー側で読めないので、判定は初回描画のあとまで保留する
  // （ここを省くと hydration mismatch になる）
  const [checked, setChecked] = useState(false);
  const [authed, setAuthed] = useState(false);

  const isPublic = PUBLIC_PATHS.includes(pathname);

  useEffect(() => {
    const hasToken = getToken() !== null;
    setAuthed(hasToken);
    setChecked(true);
    if (!hasToken && !isPublic) {
      router.replace("/login");
    }
  }, [isPublic, pathname, router]);

  if (isPublic) return <>{children}</>;
  if (!checked || !authed) return null;
  return <AppShell>{children}</AppShell>;
}
```

- [ ] **Step 2: `frontend/src/app/layout.tsx` を差し替える**

import を変更（`AppShell` の import 行を削除し、代わりに）:
```tsx
import AuthGate from "@/components/auth/auth-gate";
```
`AppShell` は `AuthGate` の中から使われるので、layout からは参照しなくなる。

`<body>` の中身を変更:
```tsx
      <body className="min-h-full bg-bg text-text font-sans antialiased">
        <AuthGate>{children}</AuthGate>
      </body>
```

- [ ] **Step 3: `frontend/src/components/account/account-panel.tsx` を作る**

```tsx
"use client";

import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import type { UserResponse } from "@/generated";
import { authApi } from "@/lib/api";
import { clearToken } from "@/lib/auth-token";

export default function AccountPanel() {
  const router = useRouter();
  const [user, setUser] = useState<UserResponse | null>(null);
  const [error, setError] = useState("");

  useEffect(() => {
    authApi
      .getMeV1AuthMeGet()
      .then(setUser)
      .catch(() => setError("アカウント情報の取得に失敗しました。"));
  }, []);

  function handleLogout() {
    clearToken();
    router.replace("/login");
  }

  return (
    <div>
      <header className="bg-surface border-b border-border px-5 sm:px-9 py-[18px]">
        <h1 className="font-serif text-[20px] font-bold tracking-[0.02em] text-text">
          アカウント
        </h1>
      </header>

      <div className="p-5 sm:p-9 max-w-[520px]">
        {error && (
          <p className="mb-5 bg-amber-lt border border-amber text-amber text-[13px] rounded-md px-3 py-2.5">
            {error}
          </p>
        )}

        <dl className="bg-surface border border-border rounded-lg divide-y divide-border">
          <div className="px-5 py-4">
            <dt className="text-[11px] font-bold text-muted tracking-[0.07em] uppercase mb-1">
              お名前
            </dt>
            <dd className="text-[15px] text-text">{user?.name ?? "—"}</dd>
          </div>
          <div className="px-5 py-4">
            <dt className="text-[11px] font-bold text-muted tracking-[0.07em] uppercase mb-1">
              メールアドレス
            </dt>
            <dd className="text-[15px] text-text break-all">{user?.email ?? "—"}</dd>
          </div>
        </dl>

        <button
          type="button"
          onClick={handleLogout}
          className="mt-6 px-4 py-2.5 rounded-md border border-border bg-surface text-[13px] font-bold text-muted transition-colors duration-150 hover:bg-navy-lt hover:border-[#CBD5E1] hover:text-navy"
        >
          ログアウト
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: `frontend/src/app/account/page.tsx` を作る**

```tsx
import AccountPanel from "@/components/account/account-panel";

export default function AccountPage() {
  return <AccountPanel />;
}
```

- [ ] **Step 5: `nav-items.tsx` に「アカウント」を足す**

`SubjectsIcon` の下にアイコンを追加:
```tsx
function AccountIcon() {
  return (
    <svg
      width="16"
      height="16"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="2"
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
    >
      <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" />
      <circle cx="12" cy="7" r="4" />
    </svg>
  );
}
```

`NAV_ITEMS` に 1 行追加:
```tsx
export const NAV_ITEMS: NavItem[] = [
  { href: "/",         label: "ホーム",     Icon: HomeIcon },
  { href: "/mistakes", label: "苦手問題",   Icon: MistakesIcon },
  { href: "/subjects", label: "科目管理",   Icon: SubjectsIcon },
  { href: "/account",  label: "アカウント", Icon: AccountIcon },
];
```

`NAV_ITEMS` はサイドバー（PC）とボトムナビ（モバイル）の唯一の定義元なので、この 1 箇所で両方に出る。

- [ ] **Step 6: 型チェック・lint・ビルド**

Run:
```bash
cd frontend && npx tsc --noEmit && npm run lint && npm run build
```
Expected: すべてエラー 0。

- [ ] **Step 7: ガードとログアウトをブラウザで確認する**

`npm run dev` と uvicorn を起動した状態で:

1. DevTools で localStorage をクリア → `http://localhost:3000/` を開く → `/login` にリダイレクトされる
2. `/mistakes` `/subjects` `/register` `/account` も同様に `/login` に飛ぶ
3. `/login` と `/signup` にはサイドバー・ボトムナビが**出ない**
4. ログインする → ホームが表示され、サイドバーに「アカウント」が出る
5. 「アカウント」を開く → 登録した名前とメールが表示される
6. 「ログアウト」を押す → `/login` に戻る。localStorage の `misnote_token` が消えている
7. ブラウザの戻るボタンで `/` に戻ろうとする → `/login` に飛ばされる
8. DevTools のコンソールに hydration 関連の警告が出ていない

Expected: 8 項目すべて成立する。

- [ ] **Step 8: 期限切れトークンの動作を確認する**

localStorage の `misnote_token` の値を手で `garbage` に書き換えてから `/` を再読み込みする。

Expected: 一瞬ホームが描画されたあと、API が 401 を返し、`api.ts` の middleware が
トークンを消して `/login` へ遷移する。

- [ ] **Step 9: コミット**

```bash
cd /Users/towa/Developer/myApp/misnote
git add frontend/src/components/auth/auth-gate.tsx frontend/src/components/account \
        frontend/src/app/account frontend/src/app/layout.tsx \
        frontend/src/components/layout/nav-items.tsx
git commit -m "$(cat <<'EOF'
feat: gate the app behind login and add an account screen

localStorage はサーバー側から読めないため、判定は Next.js の middleware
ではなくクライアント側のガードで行う。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: 通しの動作確認とドキュメント更新

**Files:**
- Modify: `docs/design/api/conventions.md`
- Modify: `docs/ROADMAP.md`
- Modify: `CLAUDE.md`
- Modify: `docs/CLAUDE.md`

**Interfaces:**
- Consumes: Task 1〜10 の成果すべて
- Produces: なし（ドキュメントのみ）

- [ ] **Step 1: テストを全件流す**

Run:
```bash
cd backend && .venv/bin/pytest -v
```
Expected: 全件 PASS。件数を記録しておく（ドキュメントには書かない）。

- [ ] **Step 2: 2 ユーザーでデータが分離されていることをブラウザで確認する**

自動テストで確認済みの内容だが、画面を通しても成立することを見る。

1. ユーザー A で登録 → `/subjects` で科目「数学」と単元「二次方程式」を作る → `/register` で問題を 1 件登録する
2. ホームと `/mistakes` にその問題が出ることを確認する
3. ログアウト → ユーザー B で新規登録
4. **ユーザー B の画面に A の科目・問題が一切出ないこと**を確認する
5. B で科目を 1 つ作る → ログアウト → A でログイン → A には B の科目が出ないこと

Expected: 5 項目すべて成立する。

- [ ] **Step 3: 復習フローが認証下でも動くことを確認する**

ユーザー A でログインした状態で、`/mistakes` の行から `/review/{id}` に入り、
「答えを見る」→「不正解」→ メモを保存 → ホームに戻る、を通す。

Expected: エラーバナーが出ず、`wrong_count` が 1 増えた状態で苦手問題一覧に反映される。

- [ ] **Step 4: `docs/design/api/conventions.md` を更新する**

- 基本情報の表: ベースURL を `http://localhost:8000/v1`（Phase 4 で `https://api.misnote.com/v1`）、
  認証方式を「FastAPI が発行する JWT（HS256・有効期限7日）。Phase 4 で Cognito に差し替え」に変更
- 認証ヘッダーの記述を `Authorization: Bearer {ログインAPIが返すJWT}` に変更
- エラーレスポンスの表から 403 の行を削除し、
  「他ユーザーのデータへのアクセスは存在を秘匿するため 404 を返す」旨の注記を足す
- エンドポイント一覧の先頭に 3 行追加:

| メソッド | パス | 説明 |
|---------|------|------|
| POST | `/auth/register` | ユーザー登録 |
| POST | `/auth/login` | ログイン（JWTを返す） |
| GET | `/auth/me` | ログイン中のユーザー情報 |

- [ ] **Step 5: `docs/ROADMAP.md` を更新する**

- フェーズ概要ブロック: Phase 3 を `✅ 完了` に、Phase 4 に `← 次はここ` を移す
- 「現在地」の段落を Phase 3 完了の内容に書き換える
- Phase 3 の「ライブラリ: `python-jose`, `passlib[bcrypt]`」を
  「ライブラリ: `python-jose`, `bcrypt`」に修正し、次の注記を足す:

> `passlib` は当初の想定だったが、bcrypt 5.0 と組み合わせるとバックエンド初期化時に
> `ValueError` で落ちるため採用しなかった。`bcrypt` を直接使っている。

- Phase 3 のチェックリスト 3 項目を `[x]` にする
- フロントエンドの記述に「ログアウトは `/account` 画面」を追記
- Phase 3 に、pytest によるバックエンドのテスト基盤を整備したことを 1 行追記する

- [ ] **Step 6: `CLAUDE.md` を更新する**

- Project overview の「There is currently no authentication: ...」の段落を、
  ローカル JWT 認証が入った状態の説明に差し替える
  （`app/deps.py::get_current_user_id()` が JWT を検証する / `app/auth.py` に
  ハッシュと JWT のユーティリティ / `app/seed.py` は無くなった / Phase 4 で Cognito）
- **「No automated test suite exists yet」の記述を削除し**、pytest の使い方に差し替える:
  - `pip install -r requirements-dev.txt` → `pytest`
  - テストは PostgreSQL の別 DB `misnote_test` に対して実行され、テストごとに巻き戻される
  - `tests/conftest.py` の `client` fixture が `get_current_user_id` を依存オーバーライドすること
    （認証まわりを触るテストは `anon_client` を使う）
- Commands セクションに `POST /v1/auth/login` でトークンを取る手順を 1 つ足す
- Backend request flow に `auth.router` → `/v1/auth` を追記
- 「All queries are scoped by `user_id` ... `units` are scoped indirectly through their
  `subject_id`」の記述が、Task 6 で実態と一致したことを反映する
- Frontend structure に `auth-gate.tsx` と `auth-token.ts` の役割を 1 行ずつ足す

- [ ] **Step 7: `docs/CLAUDE.md` を更新する**

- Architecture Summary の **Auth:** の行を、ローカル JWT（Phase 3 実装済み）＋
  Phase 4 で Cognito に差し替え予定、という内容に書き換える
- Document Map に `superpowers/specs/2026-07-31-auth-design.md` の行を足す

- [ ] **Step 8: 記述が実態と合っているか確認する**

Run:
```bash
cd /Users/towa/Developer/myApp/misnote
grep -rn "Cognito" CLAUDE.md docs/CLAUDE.md docs/design/api/conventions.md
grep -rn "passlib" docs/ CLAUDE.md backend/requirements.txt
grep -rn "seed" CLAUDE.md docs/CLAUDE.md
grep -rn "No automated test suite" CLAUDE.md
```
Expected:
- `Cognito` の残りは全て「Phase 4 の予定」の文脈になっている（現状の説明として残っていない）
- `passlib` は ROADMAP の「採用しなかった」注記のみ
- `seed` の記述が CLAUDE.md 系に残っていない
- `No automated test suite` が 0 件

- [ ] **Step 9: コミット**

```bash
cd /Users/towa/Developer/myApp/misnote
git add CLAUDE.md docs/CLAUDE.md docs/design/api/conventions.md docs/ROADMAP.md
git commit -m "$(cat <<'EOF'
docs: update docs for local JWT authentication

Cognito 前提だった記述を現状のローカル JWT に合わせ、Phase 3 を完了に
した。pytest のテスト基盤についても追記。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## 完了条件

- [ ] `cd backend && .venv/bin/pytest` が全件 PASS
- [ ] トークン無しで全 API が 401 を返す
- [ ] 新規登録 → ログイン → 全 5 画面が動作する
- [ ] 2 人のユーザーのデータが相互に見えない（単元を含む）
- [ ] ログアウト後に保護ページへアクセスすると `/login` に戻る
- [ ] `npm run build` / `npm run lint` / `npx tsc --noEmit` がすべて通る
- [ ] `backend/.env` の `SECRET_KEY` が既定値から変わっている（コミットはされていない）
